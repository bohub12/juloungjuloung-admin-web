import { createStore } from "vuex";
import theme from "./theme";
import menu from "./menu";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    theme,
    menu,
  },
});
